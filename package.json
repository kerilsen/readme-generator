{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A command-line application that dynamically generates a professional README.md file from a user's input. # Installation Initialize a repository with a `package.json` file by running `npm init -y`. Install inquirer version 8.2.4 and add to list of dependencies by running `npm i inquirer@8.2.4`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.2.4"
  },
  "keywords": []
}
